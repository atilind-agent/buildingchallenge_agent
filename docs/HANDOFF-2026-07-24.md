# Handoff — pitch-agent v2 (Closing-Loop), 2026-07-24

> **Nächste Session bitte GÜNSTIG starten:** `/model sonnet` + normales Effort, oder `/clear`.
> Der teure Denk-Teil (Vergleich mit Sebastians video-cutter, Architektur) ist erledigt und
> steckt in der Spec. Ab hier ist es TDD-Umsetzung — reine Abtipp-/Bauarbeit.

## Wo wir stehen

- **v1 fertig:** pitch-agent (Acquire: select→demo→deploy→outreach) — Code auf `main`, global
  installiert, Live-Lauf lief (7/7 Demos deployt+verifiziert), ABGABE.md ausgefüllt.
- **v2 Spec fertig + committet:** `docs/superpowers/specs/2026-07-24-pitch-agent-v2-closing-loop-design.md`
  (Umfang A, brainstorming-approved, alle 3 Design-Abschnitte bestätigt).
- **v2 Code: noch nicht geschrieben.** Bei 0 anfangen.

## Was v2 ist (Kurz)

pitch-agent v1 hört bei „Outreach-Entwurf geschrieben" auf. v2 macht den geschlossenen Kreislauf:
4 Phasen **Acquire → Advance → React → Review**. Neuer deterministischer Kern
`scripts/pipeline_engine.py` (Stage-Maschine + Cadence + CRM-QA, stdlib-only, unit-getestet);
Haiku entwirft Nudges + Antwort-Reaktionen. Alles über `leads.json`. **Details stehen vollständig
in der Spec — die ist die Wahrheit.**

## Nächste Schritte (in Reihenfolge)

1. Spec lesen: `docs/superpowers/specs/2026-07-24-pitch-agent-v2-closing-loop-design.md`.
2. **3 offene Planungs-Checks zuerst klären** (stehen am Ende der Spec, nicht raten):
   - Dashboard-Rückschreibpfad: kann `agency-dashboard/index.html` überhaupt zurückschreiben?
     (`~/.claude/agency-data/dashboard-server.log` prüfen → gibt es einen lokalen Server?)
   - `config.json`-Ort/Shape: `~/.claude/agency-data/config.json` — Cadence-Parameter dort oder
     Engine-Defaults + Override?
   - `lastTouch`-Backfill-Quelle: Feld `date` vs. Datum aus `demo-fabrik/outreach/<datum>.md`.
3. `writing-plans`-Skill → Umsetzungsplan mit Tasks erstellen (wie v1:
   `docs/superpowers/plans/`).
4. Plan Task für Task ausführen, **TDD wie v1:** Test schreiben → rot → minimal implementieren →
   grün (`python3 -m unittest discover -s tests -v`) → committen + pushen auf `main`.

## Wichtige Pfade

- Repo: `~/Desktop/buildingchallenge_agent` (Branch `main`, Remote `github.com/atilind-agent/buildingchallenge_agent`)
- Neuer Kern (zu bauen): `scripts/pipeline_engine.py` + `tests/test_pipeline_engine.py` + `tests/fixtures/leads.pipeline.sample.json`
- Bestehend, unverändert: `scripts/demo_pipeline.py` (Acquire), 20 grüne Tests
- Zu erweitern: `skills/pitch-agent/SKILL.md` (4 Phasen), `agency-dashboard/index.html` (minimal)
- Daten (alleinige Wahrheit): `~/.claude/agency-data/leads.json` (89 Leads, CRM-Schema)

## Konventionen (aus v1)

- Nur Python-Standardbibliothek, keine `requirements.txt`. Tests mit `unittest`.
- Keine echten Kundendaten ins öffentliche Repo — nur Fake-Fixtures.
- Nach JEDEM fertigen Task: committen + pushen auf `main` (Build-Journal-Regel).
- **GateGuard ist aktiv:** vor Write/Edit und erstem Bash die geforderten Fakten präsentieren.
- Kein Auto-Versand, nichts erfinden (Angaben aus echter Quelle/Fallback).

## Warnung Kosten

Diese Session endete bei ~$171 (Opus 4.8 + Effort max über langen Kontext). Für die Umsetzung
reicht Sonnet + normales Effort locker. Nicht wieder auf max/Opus für Abtipp-Arbeit.
